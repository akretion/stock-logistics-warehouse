This module may be used as a base for other modules but adds no real feature by itself.
It adds the allowed warehouses for each user, and a field to set the default warehouse of the user.
If no warehouse is manually set on a user, a default warehouse will be set instead.
